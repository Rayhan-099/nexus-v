{
  "name": "nexus-v-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend-core && npm start",
    "dev:client": "cd client-app && npm run dev",
    "dev:partner": "cd partner-dashboard && npm run dev",
    "install:all": "npm install && cd backend-core && npm install && cd ../client-app && npm install && cd ../partner-dashboard && npm install",
    "build": "echo 'Building monorepo...'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
